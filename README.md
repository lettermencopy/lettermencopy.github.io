# Lettermen Startseite – bereinigte Version

Dateien im gleichen Verzeichnis belassen:

- `index.html`
- `styles.css`
- `assets/`

Für GitHub Pages kann der Inhalt dieses Ordners direkt in das Repository kopiert werden.
Die internen Links (`/leistungen.html`, `/ueber-uns.html`, `/angebote.html`, `/impressum.html`, `/datenschutz.html`) wurden unverändert gelassen.
