# Lettermen Startseite – Problem-Sektion v2

Für GitHub:

1. `index.html` im Hauptverzeichnis ersetzen.
2. `styles.css` im Hauptverzeichnis ersetzen.
3. Den Ordner `assets` hochladen bzw. vorhandene Dateien damit überschreiben.
4. Deine bereits vorhandene `Favicon.png` im Hauptverzeichnis behalten. Die `index.html` verweist auf `/Favicon.png`.
5. Die übrigen Seiten (`angebote.html`, `leistungen.html`, `ueber-uns.html`, `datenschutz.html`, `impressum.html`, `CNAME`) nicht verändern.

Die neue Problem-Sektion ersetzt ausschließlich den bisherigen Abschnitt 3. Der CTA springt zum bestehenden finalen CTA `#kontakt`.
